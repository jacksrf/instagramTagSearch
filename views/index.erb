
<html>
  <body>
    <h1>Enter a tag that you want to search for on Instagram</h1>
    <% if url.length > 0 %>
      <h2>these are the pictures for the <%= tag %> tag:</h2>
      <% url.each do |image| %>
        <img src='<%= image %>'>
      <% end %>
    <% end %>
    <form action = "/click">
      <input type = "text" name = "name" >
        <button>Click Me</button>
    </form>
  </body>
</html>
